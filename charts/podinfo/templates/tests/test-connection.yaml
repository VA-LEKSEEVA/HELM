apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podinfo.fullname" . }}-test-connection"
  labels:
    {{- include "podinfo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ include "podinfo.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
