{{- if .Values.auth.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "podinfo.fullname" . }}-auth
  labels:
    {{- include "podinfo.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.auth.password | quote }}
{{- end }}
